---
type: daily
---

## 2026-04-02

### What I did
- Ran `yarn clean:build` to remove stale `lib/` artifacts (including orphaned `IndexedDBStorage.js`)
- Rebuilt with `yarn build` — hit 3 TS2339 errors in `WebFetcher.ts` where cheerio's `Node` type lacks `childNodes`
- Fixed by casting `node` to `any` for the `childNodes` access in the turndown table-row rule
- Fixed `WebFetcher.spec.ts` — changed `import { AxiosRequestConfig }` to `import type { AxiosRequestConfig }` to avoid ESM runtime export error
- Ran full test suite: **276 passing, 2 failing** (pre-existing proxyquire/ESM compat issue in FileFetcher.spec.ts and WebFetcher.spec.ts)

### Tasks 2-13 (see git log for full detail)
- indexName config, axios→fetch migration, FileFetcher fix, proxyquire removal, security fixes
- Protobuf codec, LocalEmbeddings, gRPC server, vectra generate, FolderWatcher
- Final state before merge: **385 passing, 0 failing**

### Task 14: Merge main into stevenic/browser-support
Merged main (11 commits ahead) into browser-support branch. Resolved 8 file conflicts:

**Conflict resolution strategy:**
- `package.json` — Combined browser additions (exports, keywords, browser entry, buffer, webpack) with main's changes (0.14.0 version, gRPC deps, optionalDeps, resolutions, build scripts)
- `FileFetcher.ts` — Took main's `fs.promises.*` pattern, kept `node:path`
- `WebFetcher.ts` — Took main's `fetch()`-based approach (better for browser compat), kept browser branch formatting
- `index.ts` — Merged both export sets (codecs, server, LocalEmbeddings, FolderWatcher + TransformersEmbeddings, TransformersTokenizer, pathUtils)
- `LocalDocumentIndex.ts` — Took main's codec-based serialization, removed local DocumentCatalog (now in codecs)
- `LocalIndex.ts` — Took main's full version (codec, options DI pattern), swapped `path` for `pathUtils`
- `LocalIndex.spec.ts` — Took main's comprehensive test suite (matches main's constructor API)
- `yarn.lock` — Reset from main and regenerated

**Additional fixes during merge:**
- Rewrote `TransformersEmbeddings.spec.ts` to remove `proxyquire` dependency (stubs `transformersModule.pipeline` directly via sinon)
- Fixed `src/codecs/migrateIndex.ts` to use `pathUtils` instead of Node `path` (VirtualFileStorage path compat on Windows)

**Results:** Clean build, **409 passing, 0 failing**

### Task 15: Commit and PR
- Committed remaining unstaged files (memory logs, README)
- Pushed 13 commits to `origin/stevenic/browser-support`
- Created PR #99: "Add browser and Electron support" targeting `main`
- PR URL: https://github.com/Stevenic/vectra/pull/99

### Task 16: Update engines.node for undici@8 compat
- Updated `package.json` engines field from `>=20.x` to `>=22.x` to match reality — `undici@8.0.0` (transitive dep) requires `node >=22.19.0`, so Node 20 is no longer viable
- CI matrix was already fixed to Node 22 only by Pipeline (commit `48f029f`); this aligns the declared minimum
