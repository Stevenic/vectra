---
type: daily
---

## 2026-04-02

### What I did
- Ran `yarn clean:build` to remove stale `lib/` artifacts (including orphaned `IndexedDBStorage.js`)
- Rebuilt with `yarn build` — hit 3 TS2339 errors in `WebFetcher.ts` where cheerio's `Node` type lacks `childNodes`
- Fixed by casting `node` to `any` for the `childNodes` access in the turndown table-row rule
- Fixed `WebFetcher.spec.ts` — changed `import { AxiosRequestConfig }` to `import type { AxiosRequestConfig }` to avoid ESM runtime export error
- Ran full test suite: **276 passing, 2 failing** (pre-existing proxyquire/ESM compat issue in FileFetcher.spec.ts and WebFetcher.spec.ts)

### Files changed
- `src/WebFetcher.ts` — cast `node` to `any` for `childNodes` access (lines 153-162)
- `src/WebFetcher.spec.ts` — changed value import to type-only import for `AxiosRequestConfig`

### Task 2: Bring `indexName` config forward from `stevenic/storage-abstraction` branch
- Added `indexName?: string` parameter to `LocalIndex` constructor (2nd positional param)
- Added `indexName?: string` to `LocalDocumentIndexConfig` interface
- `LocalDocumentIndex` passes `config.indexName` through to `super()`
- Updated all `LocalIndex` constructor calls in `LocalIndex.spec.ts` to match new signature (9 call sites)
- Clean build, **276 passing** (same 2 pre-existing proxyquire failures)

### Files changed (task 2)
- `src/LocalIndex.ts` — added `indexName` param to constructor
- `src/LocalDocumentIndex.ts` — added `indexName` to config, passed through to super
- `src/LocalIndex.spec.ts` — updated 9 constructor call sites for new param ordering

### Task 3: Replace axios with Node.js built-in fetch + bump to 0.14.0
- Removed `axios` from `dependencies` in `package.json`
- Bumped version from `0.12.3` to `0.14.0`
- Rewrote `src/OpenAIEmbeddings.ts` to use `globalThis.fetch` instead of `axios.create().post()`
  - Removed `AxiosInstance` private field, replaced with direct `fetch()` calls
  - Changed `requestConfig` type from `AxiosRequestConfig` to `RequestInit`
  - Uses `Headers` API for header merging (respects pre-supplied headers)
  - Response handling changed: `response.json()` instead of `response.data`
- Rewrote `src/WebFetcher.ts` to use `globalThis.fetch` instead of `axios.create().get()`
  - Changed `requestConfig` type from `AxiosRequestConfig` to `RequestInit`
  - Uses `response.headers.get('content-type')` instead of `response.headers['content-type']`
  - Uses `response.text()` instead of `response.data`
- Rewrote `src/OpenAIEmbeddings.spec.ts` — stubs `globalThis.fetch` via sinon instead of `axios.create`
- Rewrote `src/WebFetcher.spec.ts` — stubs `globalThis.fetch` via sinon, **eliminated proxyquire dependency**
  - This fixes 1 of the 2 pre-existing proxyquire/ESM test failures
- Clean build, **289 passing, 1 failing** (only FileFetcher.spec.ts proxyquire issue remains)

### Files changed (task 3)
- `package.json` — removed axios dep, bumped to 0.14.0
- `src/OpenAIEmbeddings.ts` — replaced axios with fetch
- `src/OpenAIEmbeddings.spec.ts` — rewrote to stub fetch
- `src/WebFetcher.ts` — replaced axios with fetch
- `src/WebFetcher.spec.ts` — rewrote to stub fetch (no more proxyquire)

### Task 4: Fix FileFetcher.spec.ts proxyquire failure
- `proxyquire` can't stub `fs/promises` because ES module namespaces are sealed (non-configurable properties)
- Changed `FileFetcher.ts` to import `fs` from `node:fs` and use `fs.promises.stat/readdir/readFile`
- Rewrote `FileFetcher.spec.ts` to stub `fs.promises.stat/readdir/readFile` directly via sinon — eliminated proxyquire
- **303 passing, 0 failing** — all tests green

### Files changed (task 4)
- `src/FileFetcher.ts` — changed `import * as fs from 'fs/promises'` to `import fs from 'node:fs'`, use `fs.promises.*`
- `src/FileFetcher.spec.ts` — replaced proxyquire with sinon stubs on `fs.promises`

### Coverage (task 4)
| Metric     | Overall |
|------------|---------|
| Statements | 97.13%  |
| Branches   | 91.32%  |
| Functions  | 97.66%  |
| Lines      | 97.70%  |

### Task 5: Remove proxyquire & rewire devDependencies + security audit
- Removed `proxyquire`, `@types/proxyquire`, `rewire`, `@types/rewire` from devDependencies (none used in source)
- Ran `yarn install` to update lockfile
- Clean build + **303 passing** confirmed
- Ran `yarn audit` — 7 production vulnerabilities, all from `cheerio > undici` (6.21.2) and `openai > @types/node-fetch > form-data` (4.0.0)

### Task 6: Fix remaining security vulnerabilities
- Added `resolutions` overrides in `package.json`:
  - `"undici": ">=6.24.0"` — fixes 6 CVEs (CVE-2026-1525/1526/1527/1528/2229/22036) from `cheerio > undici`
  - `"form-data": ">=4.0.4"` — fixes CVE-2025-7783 from `openai > @types/node-fetch > form-data`
- Upgraded `openai` from `^4.97.0` to `^4.100.0` (resolved to 4.104.0)
- Resolved versions: undici 8.0.0, form-data 4.0.5, openai 4.104.0
- Clean build, **303 passing**, `yarn audit` reports **0 vulnerabilities**

### Files changed (task 6)
- `package.json` — added resolutions for undici and form-data, bumped openai range
- `yarn.lock` — updated lockfile

### Notes
- The `requestConfig` type change (`AxiosRequestConfig` → `RequestInit`) is a breaking change for consumers passing custom axios config — hence the 0.14.0 bump.
