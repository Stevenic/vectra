---
type: daily
compressed: true
---

## Migration v0.0.0 → v0.8.0

Applied four migration steps. No daily logs or memory files existed, so v0.6.0 (compress logs), v0.7.0 (scrub system tasks), and v0.7.3 (strip version fields) were no-ops. WISDOM.md had 9 high-quality entries — all kept. The v0.8.0 step (`**/SYSTEM-PROMPT.md` added to `.teammates/.gitignore`) was already applied by a concurrent process. No files changed by this task.

## GitHub Pages setup

Added GitHub Pages infrastructure for hosting Vectra documentation.

**Files created:**
- `.github/workflows/docs.yml` — Actions workflow: builds Jekyll from `docs/`, deploys to Pages
- `docs/_config.yml` — Jekyll config using just-the-docs theme
- `docs/index.md` — Minimal landing page scaffold
- `docs/Gemfile` — Ruby dependencies for Jekyll
- `docs/.gitignore` — Excludes build artifacts

**Key decisions:**
- Used Jekyll + just-the-docs remote theme (zero Ruby infra needed in CI — `actions/jekyll-build-pages` handles it)
- Workflow triggers on `docs/**` and workflow file changes only (not all pushes)
- `cancel-in-progress: false` on concurrency group to protect in-flight deploys
- Minimal content scaffold; handed off content authoring to Scribe

**Repo setup required:** Owner must enable GitHub Pages in repo Settings → Pages → Source: "GitHub Actions"

## Language binding & npm publish workflows — ROLLED BACK

Scaffolded four publish workflows: `publish-python.yml`, `publish-csharp.yml`, `publish-rust.yml`, `publish-npm.yml`. **All rolled back by stevenic.** Direction changed: instead of publishing separate binding packages to PyPI/NuGet/crates.io, Beacon is implementing a `vectra generate` CLI command that generates bindings from the proto file directly. No separate packages to publish, no registry accounts needed.

The npm publish workflow was also removed in the rollback — to be revisited if/when needed.

## Binding publish workflow removal — confirmed already done

Beacon handed off a request to remove `publish-python.yml`, `publish-csharp.yml`, `publish-rust.yml`. Confirmed these were already removed during stevenic's earlier rollback. Only `docs.yml` exists in `.github/workflows/`. No files changed.

## CI workflow created

Added `.github/workflows/ci.yml` — the primary build/test pipeline for Vectra.

**Configuration:**
- Triggers: push to `main`, pull requests to `main`
- `paths-ignore`: docs, .teammates, markdown files (operational metadata doesn't trigger CI)
- Matrix: Node 18, 20 on ubuntu-latest
- Steps: checkout → setup-node (with yarn cache) → `yarn --frozen-lockfile` → `npm run build` → `npm run test:mocha`
- Codecov upload on Node 20 only (avoids duplicate reports)
- `fail_ci_if_error: false` on Codecov — won't block CI if Codecov is unreachable
- Uses `CODECOV_TOKEN` secret (repo owner must add this)

**Key decisions:**
- Included Node 18 in matrix per request even though `engines` field says `>=20.x`
- Ran build step separately from test (`npm run test:mocha` not `npm run test`) since `test` script re-runs build
- Coverage upload only on one matrix entry to avoid double-counting
- Least-privilege permissions: `contents: read` only

**Repo setup required:** Add `CODECOV_TOKEN` secret in repo settings for coverage uploads.

## Switched coverage from Codecov to Coveralls

Per stevenic's request (already set up on Coveralls).

**Files changed:**
- `.github/workflows/ci.yml` — Replaced `codecov/codecov-action@v4` with `coverallsapp/github-action@v2`; uses `GITHUB_TOKEN` instead of a dedicated secret
- `README.md` — Updated coverage badge from Codecov to Coveralls

**Key detail:** Coveralls action uses `GITHUB_TOKEN` (automatically available), so no extra secret setup is needed.

## CI matrix fix — Node 18 → Node 22

PR #99 build failed because the CI matrix included Node 18, but `package.json` declares `engines.node >= 20.x`. Yarn's engine check rejected the install immediately, and `fail-fast` canceled the Node 20 job too.

**Fix:** Changed matrix from `[18, 20]` to `[20, 22]`. Node 18 was never a supported version; Node 22 is current LTS.

**Files changed:**
- `.github/workflows/ci.yml` — matrix `node-version` updated

## CI matrix fix — Node 20 → Node 22 only

PR #99 build failed again. This time `undici@8.0.0` (transitive dep) requires `node >=22.19.0`, making Node 20 incompatible. Yarn's engine check rejected the install.

**Fix:** Changed matrix from `[20, 22]` to `[22]`. Updated Coveralls upload condition from `node-version == 20` to `== 22`.

**Note:** `package.json` still declares `engines.node >= 20.x` but the actual dep tree now requires Node 22+. Beacon should update the engines field.

**Files changed:**
- `.github/workflows/ci.yml` — matrix narrowed to Node 22 only

## CI fix — Coveralls "Nothing to report"

PR #99 build/test passed but the Coveralls upload step failed with "Nothing to report". Root cause: `.nycrc` only had `"reporter": ["html"]` — no lcov output for Coveralls to find.

**Fix:**
- `.nycrc` — Added `lcov` and `text` reporters alongside `html`
- `.github/workflows/ci.yml` — Added explicit `file: coverage/lcov.info` to Coveralls step

**Commit:** `d884b99`

## Added lint step to CI

Added `yarn lint` step to `.github/workflows/ci.yml`, positioned after `yarn --frozen-lockfile` install and before `npm run build`. Lint currently exits 0 (0 errors, 212 warnings) so it won't block CI, but will catch any new errors in future PRs.

**Files changed:**
- `.github/workflows/ci.yml` — Added "Lint" step running `yarn lint`

## GitHub Issues Triage for v0.14 Release

Reviewed all 18 open issues against current codebase state (v0.14.0). Categorized each as: already fixed, no longer relevant, good for v0.14 release notes/closure, or deferred to future release. Key findings:
- 6 issues are already fixed or addressed by recent work (axios removal, browser support, in-memory storage, local embeddings, watch mode, update/upsert)
- 3 issues are questions/community posts that can be closed with responses
- 4 issues are good candidates for v0.14 release notes
- 5 issues are best deferred to future releases

**No files changed** — research/analysis task only.

## GitHub Issues Cleanup — v0.14 triage execution

Closed 7 issues per triage recommendations:

**Fixed in v0.14 (closed with release notes):**
- #98 — Axios supply chain attack (removed in commit 10ff27a)
- #96 — Transformer.js support (TransformersEmbeddings shipped)
- #23 — CLI watch mode (FolderWatcher + vectra watch)
- #11 — Update metadata (updateItem/upsertItem methods)

**Questions/community posts (closed with explanations):**
- #57 — Index loaded in memory (explained storage abstraction)
- #47 — Dimension sizes (confirmed works for any dimension)
- #76 — Call for contributors (served its purpose)

**No files changed** — GitHub issue management only.

## Bumped just-the-docs theme to v0.12.0

Updated `docs/_config.yml` remote_theme pin from `v0.10.1` to `v0.12.0` (latest release as of 2026-01-23).

**Files changed:**
- `docs/_config.yml` — `remote_theme` version bumped

## Changelog generation: v0.10.0 → v0.14.0

Analyzed all commits between `v0.10.0` and `HEAD` (current `v0.14.0`). No `v0.12.3` tag exists — only `v0.10.0` and `v0.12.0`. Identified 5 release boundaries: v0.11.0, v0.11.1, v0.12.0, v0.12.2, v0.14.0.

**File created:** `.teammates/_handoffs/changelog-v0.10.0-to-v0.14.0.md` — full changelog for Scribe to format and add to docs.

## Created TYPESCRIPT-LIBRARY-PLAYBOOK.md

Analyzed the full Vectra repo structure (package.json, tsconfig, CI workflows, docs site, samples, llms.txt, README badges, ESLint config, .nycrc, .gitignore, src/ layout) and distilled it into a comprehensive playbook for any coding agent to follow when scaffolding a similar TypeScript library.

**File created:** `TYPESCRIPT-LIBRARY-PLAYBOOK.md` (~500 lines)

**Sections:** 17 sections covering interview, scaffold, package.json, tsconfig, source layout, ESLint, testing, build, git config, README/badges, contributing, docs, samples, CI/CD, llms.txt, publishing, and post-setup checklist.

## Repo gap fixes + playbook update

Applied all recommended repo gaps and updated the playbook to cover them.

**Files created:**
- `SECURITY.md` — Vulnerability reporting policy with GitHub private advisory link
- `.github/dependabot.yml` — Weekly updates for npm deps and GitHub Actions
- `.github/ISSUE_TEMPLATE/bug_report.yml` — YAML form: version, Node, OS, environment, repro steps
- `.github/ISSUE_TEMPLATE/feature_request.yml` — YAML form: use case, proposal, scope dropdown
- `.github/ISSUE_TEMPLATE/question.yml` — YAML form with docs/llms.txt links
- `.github/ISSUE_TEMPLATE/config.yml` — Disables blank issues, adds docs contact link

**Files changed:**
- `.nvmrc` — Fixed mismatch: `20` → `22` (now matches `engines.node >= 22.x` and CI matrix)
- `TYPESCRIPT-LIBRARY-PLAYBOOK.md` — Added 3 new sections (12: SECURITY.md, 13: Issue Templates, 14: Dependabot), added CODEOWNERS to Contributing section, added .nvmrc alignment note, updated scaffold tree, updated post-setup checklist, renumbered sections 12-17 → 15-20

## Added Agent Configuration Files section to playbook

Added Section 19 (Agent Configuration Files) to `TYPESCRIPT-LIBRARY-PLAYBOOK.md`. Covers auto-detection of which agent is running the playbook and generation of platform-specific config files.

**Platforms covered:** Claude Code (`CLAUDE.md`), OpenAI Codex (`AGENTS.md`), GitHub Copilot (`.github/copilot-instructions.md`), Cursor (`.cursorrules`), Windsurf (`.windsurfrules`).

**Key design:** Agent self-identifies and creates its own config file, but recommended approach is to create ALL config files so the project works for any contributor regardless of tool. Includes content template, platform-specific notes, sync strategy (canonical file + copies or symlinks), and scaffold/checklist updates.

**Files changed:**
- `TYPESCRIPT-LIBRARY-PLAYBOOK.md` — New Section 19, updated scaffold tree in Section 2, updated post-setup checklist in Section 21, renumbered sections 19-20 → 20-21
