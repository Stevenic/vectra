---
type: daily
---

## WISDOM.md compaction

Distilled WISDOM.md from 9 entries to 15 entries organized into 5 sections (General Principles, CI Pipeline, Security & Permissions, Deployment & Publishing, GitHub & Tooling). Key changes:
- Updated "CI/CD has core CI, still missing npm publish" → split into three entries: binding workflows not needed, npm publish still missing, current CI matrix details
- Added new entries from 2026-04-02 learnings: Node matrix must match dep tree, use test:mocha not test, coverage requires lcov, Coveralls uses GITHUB_TOKEN, .nvmrc alignment, docs workflow details
- Removed "Previous Projects" preamble bloat — reduced to one-liner
- Set `Last compacted: 2026-04-03`

## Merged all 10 pending Dependabot PRs

Reviewed and merged all open PRs (all from Dependabot, all CI green):

**GitHub Actions (no conflicts):**
- #101 — actions/setup-node v4→v6
- #102 — actions/checkout v4→v6
- #103 — actions/deploy-pages v4→v5

**npm dependencies (yarn.lock conflicts resolved via `@dependabot rebase`):**
- #104 — @types/node 22.15.11→25.5.0
- #105 — wink-nlp 2.3.2→2.4.0
- #106 — uuid 11.1.0→13.0.0 (major)
- #107 — cheerio 1.0.0→1.2.0
- #108 — webpack-cli 6.0.1→7.0.2 (major)
- #109 — protobufjs 7.5.4→8.0.0 (major)
- #110 — mocha 11.2.2→11.7.5

**Conflict resolution:** PRs #104-#106 merged cleanly. PRs #107-#110 developed yarn.lock conflicts from prior merges — triggered `@dependabot rebase` on each, waited for rebase, then merged sequentially. All CI checks passed post-rebase.

**No files changed locally** — all work done via GitHub API.

## Updated .nycrc to exclude untestable browser-only files

Added excludes for files that cannot be tested under Node/mocha:
- `**/templates/**` — generated template code
- `**/*.browser.ts` — browser-only variants
- `**/BrowserWebFetcher.ts` — browser-only fetcher
- `**/IndexedDBStorage.ts` — browser-only storage (IndexedDB)

**Result:** Coverage jumped from **45% → 96.07%** (statements). All 87 tests pass. The remaining ~4% gap is real uncovered code in `FolderWatcher.ts`, `TextSplitter.ts`, `IndexManager.ts`, `VectraServer.ts`, and `browser.ts`.

**File changed:** `.nycrc`
