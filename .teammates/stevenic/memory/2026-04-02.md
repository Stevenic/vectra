---
type: daily
---

# 2026-04-02

## 14:33 — Assigned @beacon
@beacon why isn't our storage abstraction being exported from the vectra package?

## 14:37 — Assigned @beacon
can you clean and re-build everything?

## 14:41 — Assigned @beacon
a couple of things before fixing broken tests... this branch shows that it's 2 commits ahead: https://github.com/Stevenic/vectra/tree/stevenic/storage-abstraction what are the missing changes?

## 14:44 — Assigned @beacon
yes

## 14:45 — Assigned @beacon
someone reported this issue: There is a compromised axios version:https://socket.dev/blog/axios-npm-package-compromisedI noticed in one of my projects that Vectra relies on it: "node_modules/vectra": {      "version": "0.12.3",      "resolved": "https://registry.npmjs.org/vectra/-/vectra-0.12.3.t...

## 14:50 — Assigned @scribe
@scribe how big of a change would it be to add support for storing the index in a more efficient format like protocol buffers?

## 14:52 — Assigned @beacon
make the change and lets bump to 0.14.0

## 14:53 — Assigned @scribe
@scribe create a spec and write it to a specs folder

## 14:56 — Assigned @scribe
create the spec and write it to a specs folder

## 14:57 — Assigned @beacon
fix the failing test and report back our current coverage

## 15:00 — Assigned @beacon
cleanup the proxyquire devDependency also scan packages for any otehr security issues

## 15:04 — Assigned @beacon
yes please fix both

## 15:09 — Assigned @scribe
1 - how large is protobufjs?
2 - include the format flag when creating the index then infer it.
3 - what's the impact on size & precision?
4 - i don't think so. I think we add a migrate function to both the api and CLI

## 15:16 — Assigned @pipeline
@pipeline add github pages support so that we can host a proper set of docs for vectra

## 15:23 — Assigned @beacon
@beacon implement the spec on protobuf support

## 15:24 — Assigned @scribe
GitHub Pages docs infrastructure is now in place under `docs/`. The site uses Jekyll with the just-the-docs remote theme (configured in `docs/_config.yml`). The landing page is at `docs/index.md`.

Your task: Create the documentation content pages for the Vectra docs site. Use the README.md as yo...

## 15:34 — Assigned @scribe
@scribe I want to remove the link to vectra-py and instead create a bindings folder with native language bindings for popular programming languages. We can use the CLI to load and host the index but given how expensive it is to load an index we want to keep it loaded. what's the best way to appro...

## 15:35 — Assigned @beacon
add support for local embeddings using transformer.js. make it an optional dependency and look at ..\recall\packages\recall for an example implementation

## 15:51 — Assigned @scribe
1 - Just support both
2 - server side
3 - I'd say support both again
4 - Local host for now.

Language wise Python, C#, and Rust

## 15:56 — Assigned @beacon
hand off to beacon

## 16:02 — Assigned @scribe
1 - gitignore is fine
2 - we should have pipeline install all of the CLI's needed to publish the bindings and I'll create accounts on the various services.
3 - auto-detect
4 - yes

yes handoff

## 16:03 — Assigned @pipeline
Set up CI/CD workflows for publishing the Vectra language bindings to their respective package registries. The spec is at `specs/grpc-server-and-bindings.md` (§8 for binding structure, §10 Q2 for the publishing decision).

What's needed:
1. A GitHub Actions workflow (or workflows) that install th...

## 16:08 — Assigned @pipeline
can we add a pipeline for npm as well?

## 16:11 — Assigned @beacon
Implement the gRPC server and language bindings per the approved spec at `specs/grpc-server-and-bindings.md`. Start with **Phase 1: Proto + Server Core** only. This includes:

1. Create `proto/vectra_service.proto` (full definition is in spec §5)
2. Implement `src/server/VectraServer.ts`, `src/se...

## 16:16 — Assigned @beacon
question... are there any physical bindings that need to be published? how does this get setup? could we just have the cli generate language bindings and document that?

## 16:21 — Assigned @pipeline
@pipeline I just rolled back your changes publishing pipelines. we're going a different direction

## 16:27 — Assigned @beacon
lets do that
