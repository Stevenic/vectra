[package]
name = "vectra-client"
version = "0.1.0"
edition = "2021"
description = "Vectra gRPC client for Rust"
license = "MIT"

[dependencies]
tonic = "0.12"
prost = "0.13"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[build-dependencies]
tonic-build = "0.12"
